# ============================================================================
# Advanced Arabic-Python Test Program
# Showcases all language features and VM capabilities
# ============================================================================

# ─────────────────────────────────────────────────────────────────────────────
# 1. BASIC ARITHMETIC & VARIABLES
# ─────────────────────────────────────────────────────────────────────────────
print("=== Arithmetic Operations ===")

# Integer operations
أ = 10
ب = 20
مجموع = أ + ب
print("10 + 20 =", مجموع)

# Constant folding optimization (should be optimized to 50)
نتيجة_ثابتة = 2 + 3 * 5 + 4 * 10 - 7
print("Complex expression:", نتيجة_ثابتة)

# Float operations
عدد_عشري_١ = 3.14
عدد_عشري_٢ = 2.71
print("Float division:", عدد_عشري_١ / عدد_عشري_٢)

# Power operation
قوة = 2 ** 10
print("2^10 =", قوة)

# ─────────────────────────────────────────────────────────────────────────────
# 2. STRINGS & CONCATENATION
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== String Operations ===")

اسم = "أحمد"
تحية = "مرحبا يا " + اسم
print(تحية)

# String repetition
نجوم = "*" * 10
print(نجوم)

# ─────────────────────────────────────────────────────────────────────────────
# 3. LISTS & COLLECTIONS
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== Lists ===")

# List creation
أرقام = [1، 2، 3، 4، 5]
print("List:", أرقام)

# List operations
print("First element:", أرقام[0])
print("Last element:", أرقام[-1])
print("Length:", len(أرقام))

# List concatenation
قائمة_أخرى = [6، 7، 8]
قائمة_كاملة = أرقام + قائمة_أخرى
print("Combined list:", قائمة_كاملة)

# ─────────────────────────────────────────────────────────────────────────────
# 4. CONTROL FLOW - IF/ELSE
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== Conditionals ===")

عمر = 25

اذا عمر < 18:
    print("قاصر")
والااذا عمر < 60:
    print("بالغ")
والا:
    print("كبير السن")

# Comparison chaining
س = 15
اذا 10 < س و س < 20:
    print("س بين 10 و 20")

# ─────────────────────────────────────────────────────────────────────────────
# 5. LOOPS - WHILE
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== While Loop ===")

عداد = 1
مجموع_تراكمي = 0

طالما عداد <= 5:
    مجموع_تراكمي = مجموع_تراكمي + عداد
    print("Counter:", عداد، "Sum:", مجموع_تراكمي)
    عداد = عداد + 1

# ─────────────────────────────────────────────────────────────────────────────
# 6. LOOPS - FOR
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== For Loop ===")

# Iterate over list
لكل رقم في [1، 2، 3، 4، 5]:
    مربع = رقم * رقم
    print("Square of", رقم, "is", مربع)

# Range iteration
print("\nUsing range:")
لكل i في range(5):
    print("Index:", i)

# ─────────────────────────────────────────────────────────────────────────────
# 7. FUNCTIONS
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== Functions ===")

# Simple function
عرف جمع(أ، ب):
    return أ + ب

نتيجة_جمع = جمع(15، 25)
print("15 + 25 =", نتيجة_جمع)

# Recursive function - Fibonacci
عرف فيبوناتشي(ن):
    اذا ن <= 1:
        return ن
    return فيبوناتشي(ن - 1) + فيبوناتشي(ن - 2)

print("Fibonacci(10):", فيبوناتشي(10))

# Factorial
عرف مضروب(ن):
    اذا ن <= 1:
        return 1
    return ن * مضروب(ن - 1)

print("Factorial(5):", مضروب(5))

# ─────────────────────────────────────────────────────────────────────────────
# 8. BUILT-IN FUNCTIONS
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== Built-in Functions ===")

أرقام_للاختبار = [3، 1، 4، 1، 5، 9، 2، 6]

print("Original list:", أرقام_للاختبار)
print("Length:", len(أرقام_للاختبار))
print("Min:", min(أرقام_للاختبار))
print("Max:", max(أرقام_للاختبار))
print("Sum:", sum(أرقام_للاختبار))

مرتبة = sorted(أرقام_للاختبار)
print("Sorted:", مرتبة)

معكوسة = reversed(أرقام_للاختبار)
print("Reversed:", معكوسة)

# ─────────────────────────────────────────────────────────────────────────────
# 9. TYPE CONVERSIONS
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== Type Conversions ===")

نص_رقم = "42"
رقم_محول = int(نص_رقم)
print("String to int:", رقم_محول)

رقم_للتحويل = 3.14159
نص_محول = str(رقم_للتحويل)
print("Float to string:", نص_محول)

print("Type of 42:", type(42))
print("Type of 3.14:", type(3.14))
print("Type of 'hello':", type("hello"))

# ─────────────────────────────────────────────────────────────────────────────
# 10. BOOLEAN OPERATIONS
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== Boolean Logic ===")

صحيح_١ = صحيح
خطا_١ = خطا

print("True and False:", صحيح_١ و خطا_١)
print("True or False:", صحيح_١ او خطا_١)
print("Not True:", ليس صحيح_١)

# All and any
قائمة_صحيح = [صحيح، صحيح، صحيح]
قائمة_مختلطة = [صحيح، خطا، صحيح]

print("all([True, True, True]):", all(قائمة_صحيح))
print("all([True, False, True]):", all(قائمة_مختلطة))
print("any([True, False, True]):", any(قائمة_مختلطة))

# ─────────────────────────────────────────────────────────────────────────────
# 11. ADVANCED LIST OPERATIONS
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== Advanced Lists ===")

# Enumerate
مدن = ["الرياض"، "جدة"، "الدمام"]
لكل فهرس_مدينة في enumerate(مدن):
    print("Index:", فهرس_مدينة[0], "City:", فهرس_مدينة[1])

# Zip
أسماء = ["أحمد"، "محمد"، "علي"]
أعمار = [25، 30، 28]
مدمجة = zip(أسماء، أعمار)
print("Zipped:", مدمجة)

# ─────────────────────────────────────────────────────────────────────────────
# 12. MATHEMATICAL OPERATIONS
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== Math Operations ===")

# Absolute value
print("abs(-42):", abs(-42))

# Power
print("pow(2, 8):", pow(2، 8))

# Square root
print("sqrt(16):", sqrt(16))

# Rounding
print("round(3.7):", round(3.7))

# ─────────────────────────────────────────────────────────────────────────────
# 13. COMPLEX ALGORITHMS
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== Algorithms ===")

# Prime number checker
عرف هل_عدد_اولي(ن):
    اذا ن < 2:
        return خطا
    
    مقسوم = 2
    طالما مقسوم * مقسوم <= ن:
        اذا ن % مقسوم == 0:
            return خطا
        مقسوم = مقسوم + 1
    
    return صحيح

print("Is 17 prime?", هل_عدد_اولي(17))
print("Is 18 prime?", هل_عدد_اولي(18))

# Find primes up to N
عرف أعداد_أولية_حتى(ن):
    أعداد = []
    عدد = 2
    طالما عدد <= ن:
        اذا هل_عدد_اولي(عدد):
            أعداد = أعداد + [عدد]
        عدد = عدد + 1
    return أعداد

أعداد_أولية = أعداد_أولية_حتى(20)
print("Primes up to 20:", أعداد_أولية)

# ─────────────────────────────────────────────────────────────────────────────
# 14. OPTIMIZATION SHOWCASE
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== Optimization Tests ===")

# Constant folding (should be optimized at compile time)
ثابت_١ = 100 * 2 + 50 - 10 * 5
print("Constant expression (optimized):", ثابت_١)

# Algebraic simplification (x * 1 = x, x + 0 = x)
قيمة = 42
نتيجة_بسيطة = قيمة * 1 + 0
print("Simplified expression:", نتيجة_بسيطة)

# Dead code elimination (unreachable code)
اذا صحيح:
    print("This will execute")
والا:
    # This dead code should be eliminated
    print("This won't execute")

# Strength reduction (x * 2 → x + x)
رقم_للضرب = 100
نتيجة_ضرب = رقم_للضرب * 2
print("Strength reduction:", نتيجة_ضرب)

# ─────────────────────────────────────────────────────────────────────────────
# 15. PERFORMANCE TEST
# ─────────────────────────────────────────────────────────────────────────────
print("\n=== Performance Test ===")

# Sum of squares
عرف مجموع_المربعات(ن):
    مجموع = 0
    عداد = 1
    طالما عداد <= ن:
        مجموع = مجموع + عداد * عداد
        عداد = عداد + 1
    return مجموع

نتيجة_مربعات = مجموع_المربعات(100)
print("Sum of squares (1 to 100):", نتيجة_مربعات)

# ─────────────────────────────────────────────────────────────────────────────
# 16. FINAL MESSAGE
# ─────────────────────────────────────────────────────────────────────────────
print("\n" + "=" * 50)
print("✓ All tests completed successfully!")
print("✓ Arabic-Python VM is working perfectly!")
print("=" * 50)